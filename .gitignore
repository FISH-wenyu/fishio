# Dependencies
node_modules/

# Secrets — never commit
.env
.env.local

# Runtime state (per-machine; contains play history, NCM cookie, queue, etc.)
state/state.json
state/*.db
state/*.log
state/*.out.log
state/*.err.log
state/*.err
state/*.out
state/logs/

# Per-session hand-off notes — generated locally for AI agents to resume context,
# not meant for the public repo. AGENTS.md is the long-term project memory; these
# session notes are point-in-time snapshots.
docs/session-*.md

# Hash-keyed TTS cache (can be regenerated)
cache/

# Logs
*.log
cloudflared.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Build / coverage (none today but reserved)
dist/
build/
coverage/
